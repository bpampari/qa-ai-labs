services:
  mcp:
    image: ghcr.io/modelcontextprotocol/server:latest
    container_name: mcp-server
    ports:
      - "3333:3333"
    volumes:
      - ../selenium-ai-framework:/workspace
    environment:
      - MCP_WORKSPACE=/workspace
